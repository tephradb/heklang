# Devenv
.devenv*
devenv.local.nix
devenv.local.yaml

# direnv
.direnv

# pre-commit
.pre-commit-config.yaml


# Added by cargo

/target

# tree-sitter; src/ is committed, the built object and node deps are not
/tree-sitter-hek/hek.so
/tree-sitter-hek/node_modules
